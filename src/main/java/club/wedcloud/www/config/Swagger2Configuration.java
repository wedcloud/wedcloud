package club.wedcloud.www.config;

import org.springframework.context.annotation.Configuration;

@Configuration
// @EnableSwagger2
public class Swagger2Configuration {

  // @Bean
  // public Docket createRestApi() {
  // return new Docket(DocumentationType.SWAGGER_2).apiInfo(apiInfo()).select()
  // .apis(RequestHandlerSelectors.basePackage("club.wedcloud.www.controller"))// 这是注意的代码
  // .paths(PathSelectors.any()).build();
  // }
  //
  // private ApiInfo apiInfo() {
  // return new ApiInfoBuilder().title("wedcloud接口文档").description("wedcloud相关接口的文档")
  // .termsOfServiceUrl("http://www.wedcloud.club").version("1.0").build();
  // }
}
